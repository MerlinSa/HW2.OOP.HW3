package main.drugstore.components;

import main.drugstore.Component;

public class Water extends Component { //наследник
    public Water(String title, Double weight, int power) {
        super(title, weight, power);
    }
}
